export const openPdfBase64 = (base64Url, fallbackName = 'Document.pdf') => { if (!base64Url) return; if (base64Url.startsWith('http')) { window.open(base64Url, '_blank'); return; } try { const parts = base64Url.split(';base64,'); const contentType = parts[0].split(':')[1]; const raw = window.atob(parts[1]); const rawLength = raw.length; const uInt8Array = new Uint8Array(rawLength); for (let i = 0; i < rawLength; ++i) { uInt8Array[i] = raw.charCodeAt(i); } const blob = new Blob([uInt8Array], { type: contentType || 'application/pdf' }); const blobUrl = URL.createObjectURL(blob); window.open(blobUrl, '_blank'); setTimeout(() => URL.revokeObjectURL(blobUrl), 10000); } catch (err) { console.error('Failed to open PDF blob', err); window.open(base64Url, '_blank'); } };